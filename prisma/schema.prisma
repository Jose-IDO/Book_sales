generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = env("DATABASE_URL")
}

model Book {
  id          String        @id @default(cuid())
  /** 8-digit string for Firestore bookRegistry / bookReserve keys (unique). */
  registryEightDigitId String? @unique
  title       String
  author      String
  description String
  isbn        String
  priceCents  Int
  stock       Int           @default(1)
  isAvailable   Boolean       @default(true)
  createdAt     DateTime      @default(now())
  reservations  Reservation[]
}

model Reservation {
  id          String            @id @default(cuid())
  bookId      String
  book        Book              @relation(fields: [bookId], references: [id], onDelete: Cascade)
  /** Firebase Auth uid when reservation was created with a verified ID token. */
  firebaseUid String?
  buyerName   String
  buyerEmail  String
  message     String?
  proofPath   String
  proofMime   String
  status      ReservationStatus @default(PENDING)
  createdAt   DateTime          @default(now())
  updatedAt   DateTime          @updatedAt
}

enum ReservationStatus {
  PENDING
  APPROVED
  REJECTED
}
